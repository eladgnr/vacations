from django.apps import AppConfig

""" Vacations app configuration.
This module defines the configuration for the Vacations app"""


class VacationsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'vacations'
